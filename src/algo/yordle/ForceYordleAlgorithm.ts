export class ForceYordleAlgorithm {
    private readonly YORDLES: string[]=[
        'Lulu',
        'Rumble',
        'Tristana',
        'Teemo',
        'Poppy'
    ];
    public executeStep(inputs: Record<string, string>): void {
        for (const [key, value] of Object.entries(inputs)) {

        }

    }
}